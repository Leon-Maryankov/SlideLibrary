services:
  slidebrary:
    image: ghcr.io/leon-maryankov/slidelibrary:latest
    ports:
      - "3001:3001"
    volumes:
      - ./assets:/app/assets
    environment:
      - SOFFICE_PATH=/usr/bin/soffice
      - PDFTOPPM_PATH=/usr/bin/pdftoppm
    restart: unless-stopped
